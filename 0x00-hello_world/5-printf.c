#include <stdio.h>

/**
 * main - this is main function used
 *
 * Return: 0 always when correct
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art");
	return (0);
}
